apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: express-buildah-app
spec:
  tls:
    termination: edge
  to:
    kind: Service
    name: express-buildah-app
    weight: 100
  wildcardPolicy: None
